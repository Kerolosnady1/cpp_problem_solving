#include <iostream>
#include <string>
using namespace std;

int main()
{
    // 19April25
    // Program:
    /*convert the string sequence to integers.*/
    string s = "Kerolos";

    int stringint;

    for (int i = 0; i < s.size(); ++i)
    {
        if (s[i] == s[i])
        {
            stringint = s[i];
        }
        cout << "The Output After Converting Is: " << stringint << endl;
    }
}